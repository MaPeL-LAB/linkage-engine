name: build-model-portfolio-foundation-once

on:
  push:
    branches:
      - agent/model-portfolio-recipe-orchestration

permissions:
  contents: write

env:
  PYTHONHASHSEED: "0"
  MAPEL_TEST_DATA_POLICY: synthetic_only
  MAPEL_TEST_SEED: "20260816"
  OMP_NUM_THREADS: "1"
  MKL_NUM_THREADS: "1"
  OPENBLAS_NUM_THREADS: "1"

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout implementation branch
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: agent/model-portfolio-recipe-orchestration
          fetch-depth: 0
      - name: Set up Python 3.12
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.12"
          cache: pip
      - name: Write bounded portfolio and recipe foundation
        run: |
          rm .github/workflows/build-model-portfolio-foundation-once.yml
          python - <<'PY'
          from pathlib import Path

          root = Path('.')

          (root / 'src/mapel_linkage/pipeline/model_portfolio.py').write_text(r'''"""Typed model-portfolio contracts for bounded champion/challenger development."""

from __future__ import annotations

import hashlib
import json
from typing import Annotated, ClassVar, Literal, Self

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, model_validator

from mapel_linkage.configuration.models import LinkageConfig

Identifier = Annotated[
    StrictStr,
    Field(min_length=1, max_length=128, pattern=r"^[A-Za-z][A-Za-z0-9_.-]*$"),
]

PairModelFamily = Literal[
    "fellegi_sunter",
    "xgboost",
    "lightgbm",
    "pytorch",
    "stacking",
]
PairModelImplementation = Literal[
    "mapel_reference_fellegi_sunter",
    "xgboost_classifier",
    "lightgbm_classifier",
    "pytorch_pair_mlp",
    "stacking_logistic",
]
PairModelRole = Literal["baseline", "challenger", "ensemble", "shadow"]
RankerFamily = Literal["xgboost", "lightgbm"]
RankerImplementation = Literal["xgboost_ranker", "lightgbm_ranker"]
ArtifactFormat = Literal[
    "package_json",
    "xgboost_json",
    "lightgbm_text",
    "pytorch_state_dict",
]


class PortfolioNode(BaseModel):
    """Strict immutable portfolio node; configuration remains non-executable data."""

    model_config: ClassVar[ConfigDict] = ConfigDict(
        extra="forbid",
        frozen=True,
        validate_default=True,
        hide_input_in_errors=True,
    )


class PairModelCandidateDeclaration(PortfolioNode):
    """One eligible pair-scoring candidate in a development tournament."""

    model_id: Identifier
    family: PairModelFamily
    implementation: PairModelImplementation
    role: PairModelRole
    enabled: StrictBool = True
    require_verified_labels: StrictBool
    artifact_format: ArtifactFormat
    base_model_ids: Annotated[tuple[Identifier, ...], Field(max_length=16)] = ()
    decision_authority: Literal["evidence_only"] = "evidence_only"
    merge_authority: Literal["none"] = "none"

    @model_validator(mode="after")
    def validate_family_contract(self) -> Self:
        expected = {
            "mapel_reference_fellegi_sunter": "fellegi_sunter",
            "xgboost_classifier": "xgboost",
            "lightgbm_classifier": "lightgbm",
            "pytorch_pair_mlp": "pytorch",
            "stacking_logistic": "stacking",
        }[self.implementation]
        if self.family != expected:
            raise ValueError("Pair-model family and implementation must agree.")
        if self.family == "fellegi_sunter" and self.require_verified_labels:
            raise ValueError("The reference Fellegi-Sunter baseline must not require labels to fit.")
        if self.family != "stacking" and self.base_model_ids:
            raise ValueError("Only stacking candidates may declare base models.")
        if self.family == "stacking" and len(set(self.base_model_ids)) < 2:
            raise ValueError("A stacking candidate requires at least two distinct base models.")
        return self


class RankingCandidateDeclaration(PortfolioNode):
    """One candidate ranker; ranking never receives relationship authority."""

    model_id: Identifier
    family: RankerFamily
    implementation: RankerImplementation
    enabled: StrictBool = True
    query_side: Literal["source", "target"]
    top_k: Annotated[StrictInt, Field(gt=0, le=1000)]
    require_verified_labels: Literal[True] = True
    artifact_format: Literal["xgboost_json", "lightgbm_text"]
    ranking_authority: Literal["ordering_only"] = "ordering_only"
    decision_authority: Literal["none"] = "none"
    merge_authority: Literal["none"] = "none"

    @model_validator(mode="after")
    def validate_family_contract(self) -> Self:
        expected = {
            "xgboost_ranker": "xgboost",
            "lightgbm_ranker": "lightgbm",
        }[self.implementation]
        if self.family != expected:
            raise ValueError("Ranking family and implementation must agree.")
        return self


class ModelPortfolioDeclaration(PortfolioNode):
    """Privacy-safe shortlist for a protected model-development tournament."""

    portfolio_id: Identifier
    pair_candidates: Annotated[
        tuple[PairModelCandidateDeclaration, ...], Field(min_length=1, max_length=16)
    ]
    ranking_candidates: Annotated[tuple[RankingCandidateDeclaration, ...], Field(max_length=8)] = ()
    mandatory_baseline_id: Identifier
    maximum_challengers: Annotated[StrictInt, Field(ge=0, le=8)] = 3
    allow_shadow_scoring: Literal[True] = True
    test_partition_may_select_portfolio: Literal[False] = False
    recommendation_authority: Literal["none"] = "none"
    decision_authority: Literal["none"] = "none"
    merge_authority: Literal["none"] = "none"

    @model_validator(mode="after")
    def validate_portfolio(self) -> Self:
        pair_ids = [candidate.model_id for candidate in self.pair_candidates]
        ranker_ids = [candidate.model_id for candidate in self.ranking_candidates]
        all_ids = pair_ids + ranker_ids
        if len(all_ids) != len(set(all_ids)):
            raise ValueError("Model-portfolio identifiers must be unique.")
        baseline = next(
            (candidate for candidate in self.pair_candidates if candidate.model_id == self.mandatory_baseline_id),
            None,
        )
        if baseline is None or not baseline.enabled or baseline.role != "baseline":
            raise ValueError("The mandatory baseline must identify one enabled baseline candidate.")
        if sum(candidate.role == "baseline" for candidate in self.pair_candidates) != 1:
            raise ValueError("A model portfolio must contain exactly one baseline.")
        pair_id_set = set(pair_ids)
        for candidate in self.pair_candidates:
            if candidate.family != "stacking":
                continue
            if candidate.model_id in candidate.base_model_ids:
                raise ValueError("A stacking candidate cannot include itself as a base model.")
            if not set(candidate.base_model_ids).issubset(pair_id_set):
                raise ValueError("Stacking base models must exist in the same portfolio.")
        return self

    @property
    def portfolio_digest(self) -> str:
        payload = self.model_dump(mode="json")
        encoded = json.dumps(payload, sort_keys=True, separators=(",", ":")).encode("utf-8")
        return hashlib.sha256(encoded).hexdigest()

    def safe_summary(self) -> dict[str, int | str | bool]:
        return {
            "portfolio_id": self.portfolio_id,
            "portfolio_digest": self.portfolio_digest,
            "pair_candidate_count": len(self.pair_candidates),
            "enabled_pair_candidate_count": sum(item.enabled for item in self.pair_candidates),
            "ranking_candidate_count": len(self.ranking_candidates),
            "maximum_challengers": self.maximum_challengers,
            "allow_shadow_scoring": self.allow_shadow_scoring,
            "test_partition_may_select_portfolio": self.test_partition_may_select_portfolio,
            "recommendation_authority": self.recommendation_authority,
            "decision_authority": self.decision_authority,
            "merge_authority": self.merge_authority,
        }


def compile_model_portfolio(config: LinkageConfig) -> ModelPortfolioDeclaration:
    """Normalize the current configuration into a portfolio-ready contract.

    The present project schema still exposes one boosted, one neural, and one ranking
    declaration. This compiler creates the plural contract consumed by later orchestration,
    while preserving existing example configurations. A later schema revision may expose
    the plural declaration directly without changing this downstream contract.
    """

    pair_candidates: list[PairModelCandidateDeclaration] = []
    fs = config.models.fellegi_sunter
    pair_candidates.append(
        PairModelCandidateDeclaration(
            model_id=fs.model_id,
            family="fellegi_sunter",
            implementation="mapel_reference_fellegi_sunter",
            role="baseline",
            enabled=fs.enabled,
            require_verified_labels=False,
            artifact_format="package_json",
        )
    )
    boosted = config.models.boosted_tree
    if boosted is not None:
        family: Literal["xgboost", "lightgbm"] = (
            "xgboost" if boosted.implementation == "xgboost_classifier" else "lightgbm"
        )
        artifact_format: Literal["xgboost_json", "lightgbm_text"] = (
            "xgboost_json" if family == "xgboost" else "lightgbm_text"
        )
        pair_candidates.append(
            PairModelCandidateDeclaration(
                model_id=boosted.model_id,
                family=family,
                implementation=boosted.implementation,
                role="challenger",
                enabled=boosted.enabled,
                require_verified_labels=boosted.require_verified_labels,
                artifact_format=artifact_format,
            )
        )
    neural = config.models.neural
    if neural is not None:
        pair_candidates.append(
            PairModelCandidateDeclaration(
                model_id=neural.model_id,
                family="pytorch",
                implementation=neural.implementation,
                role="challenger",
                enabled=neural.enabled,
                require_verified_labels=neural.require_verified_labels,
                artifact_format="pytorch_state_dict",
            )
        )
    ranking_candidates: list[RankingCandidateDeclaration] = []
    ranking = config.models.ranking
    if ranking is not None:
        rank_family: Literal["xgboost", "lightgbm"] = (
            "xgboost" if ranking.implementation == "xgboost_ranker" else "lightgbm"
        )
        rank_format: Literal["xgboost_json", "lightgbm_text"] = (
            "xgboost_json" if rank_family == "xgboost" else "lightgbm_text"
        )
        ranking_candidates.append(
            RankingCandidateDeclaration(
                model_id=ranking.model_id,
                family=rank_family,
                implementation=ranking.implementation,
                enabled=ranking.enabled,
                query_side=ranking.query_side,
                top_k=ranking.top_k,
                artifact_format=rank_format,
            )
        )
    enabled_challengers = sum(
        candidate.enabled and candidate.role != "baseline" for candidate in pair_candidates
    )
    return ModelPortfolioDeclaration(
        portfolio_id="compiled_model_portfolio",
        pair_candidates=tuple(pair_candidates),
        ranking_candidates=tuple(ranking_candidates),
        mandatory_baseline_id=fs.model_id,
        maximum_challengers=min(3, enabled_challengers),
    )


__all__ = [
    "ModelPortfolioDeclaration",
    "PairModelCandidateDeclaration",
    "RankingCandidateDeclaration",
    "compile_model_portfolio",
]
''', encoding='utf-8')

          (root / 'src/mapel_linkage/pipeline/recipe_io.py').write_text(r'''"""Strict non-executable JSON serialization for approved pipeline recipes."""

from __future__ import annotations

import json
from collections.abc import Iterable

from mapel_linkage.domain.errors import PipelineError
from mapel_linkage.pipeline.recipes import (
    OperationalValidationStatus,
    PipelineRecipeArtifact,
    RecipeApprovalStatus,
)

_SCHEMA_VERSION = "1"
_MAX_RECIPE_BYTES = 262_144
_EXPECTED_KEYS = {
    "schema_version",
    "recipe_id",
    "recipe_version",
    "linkage_mode",
    "assignment_constraint",
    "configuration_digest",
    "candidate_plan_digest",
    "feature_schema_digest",
    "champion_model_id",
    "champion_model_version",
    "champion_artifact_digest",
    "calibrator_digest",
    "ranking_artifact_digest",
    "decision_policy_digest",
    "validation_evidence_digest",
    "approval_status",
    "operational_validation",
    "decision_authority",
    "merge_authority",
    "recipe_digest",
}


def _reject_duplicate_keys(pairs: Iterable[tuple[str, object]]) -> dict[str, object]:
    result: dict[str, object] = {}
    for key, value in pairs:
        if key in result:
            raise PipelineError("ML-RECIPE-006", "A pipeline recipe contains duplicate keys.")
        result[key] = value
    return result


def pipeline_recipe_payload(recipe: PipelineRecipeArtifact) -> dict[str, object]:
    """Return the canonical aggregate-only recipe payload."""
    return {
        "schema_version": _SCHEMA_VERSION,
        "recipe_id": recipe.recipe_id,
        "recipe_version": recipe.recipe_version,
        "linkage_mode": recipe.linkage_mode,
        "assignment_constraint": recipe.assignment_constraint,
        "configuration_digest": recipe.configuration_digest,
        "candidate_plan_digest": recipe.candidate_plan_digest,
        "feature_schema_digest": recipe.feature_schema_digest,
        "champion_model_id": recipe.champion_model_id,
        "champion_model_version": recipe.champion_model_version,
        "champion_artifact_digest": recipe.champion_artifact_digest,
        "calibrator_digest": recipe.calibrator_digest,
        "ranking_artifact_digest": recipe.ranking_artifact_digest,
        "decision_policy_digest": recipe.decision_policy_digest,
        "validation_evidence_digest": recipe.validation_evidence_digest,
        "approval_status": recipe.approval_status.value,
        "operational_validation": recipe.operational_validation.value,
        "decision_authority": recipe.decision_authority,
        "merge_authority": recipe.merge_authority,
        "recipe_digest": recipe.recipe_digest,
    }


def serialize_pipeline_recipe(recipe: PipelineRecipeArtifact) -> str:
    """Serialize without code objects, paths, rows, identifiers, or candidate pairs."""
    return json.dumps(
        pipeline_recipe_payload(recipe),
        sort_keys=True,
        separators=(",", ":"),
    ) + "\n"


def deserialize_pipeline_recipe(payload: str) -> PipelineRecipeArtifact:
    """Load a strict recipe and detect unknown keys, duplicates, and tampering."""
    if len(payload.encode("utf-8")) > _MAX_RECIPE_BYTES:
        raise PipelineError("ML-RECIPE-007", "The pipeline recipe exceeds its size limit.")
    try:
        raw = json.loads(payload, object_pairs_hook=_reject_duplicate_keys)
    except PipelineError:
        raise
    except (TypeError, ValueError, json.JSONDecodeError):
        raise PipelineError("ML-RECIPE-008", "The pipeline recipe is not valid JSON.") from None
    if not isinstance(raw, dict):
        raise PipelineError("ML-RECIPE-009", "The pipeline recipe must be a JSON object.")
    if set(raw) != _EXPECTED_KEYS:
        raise PipelineError("ML-RECIPE-010", "The pipeline recipe schema is invalid.")
    if raw.get("schema_version") != _SCHEMA_VERSION:
        raise PipelineError("ML-RECIPE-011", "The pipeline recipe schema version is unsupported.")
    expected_digest = raw.get("recipe_digest")
    try:
        recipe = PipelineRecipeArtifact(
            recipe_id=str(raw["recipe_id"]),
            recipe_version=str(raw["recipe_version"]),
            linkage_mode=raw["linkage_mode"],  # type: ignore[arg-type]
            assignment_constraint=raw["assignment_constraint"],  # type: ignore[arg-type]
            configuration_digest=str(raw["configuration_digest"]),
            candidate_plan_digest=str(raw["candidate_plan_digest"]),
            feature_schema_digest=str(raw["feature_schema_digest"]),
            champion_model_id=str(raw["champion_model_id"]),
            champion_model_version=str(raw["champion_model_version"]),
            champion_artifact_digest=str(raw["champion_artifact_digest"]),
            calibrator_digest=str(raw["calibrator_digest"]),
            ranking_artifact_digest=(
                None
                if raw["ranking_artifact_digest"] is None
                else str(raw["ranking_artifact_digest"])
            ),
            decision_policy_digest=str(raw["decision_policy_digest"]),
            validation_evidence_digest=str(raw["validation_evidence_digest"]),
            approval_status=RecipeApprovalStatus(str(raw["approval_status"])),
            operational_validation=OperationalValidationStatus(
                str(raw["operational_validation"])
            ),
            decision_authority=raw["decision_authority"],  # type: ignore[arg-type]
            merge_authority=raw["merge_authority"],  # type: ignore[arg-type]
        )
    except (KeyError, TypeError, ValueError, PipelineError):
        raise PipelineError("ML-RECIPE-010", "The pipeline recipe schema is invalid.") from None
    if not isinstance(expected_digest, str) or recipe.recipe_digest != expected_digest:
        raise PipelineError("ML-RECIPE-012", "The pipeline recipe integrity check failed.")
    return recipe


__all__ = [
    "deserialize_pipeline_recipe",
    "pipeline_recipe_payload",
    "serialize_pipeline_recipe",
]
''', encoding='utf-8')

          (root / 'tests/pipeline/test_model_portfolio.py').write_text(r'''from __future__ import annotations

import pytest
from pydantic import ValidationError

from mapel_linkage.configuration import load_config
from mapel_linkage.pipeline.model_portfolio import (
    ModelPortfolioDeclaration,
    PairModelCandidateDeclaration,
    RankingCandidateDeclaration,
    compile_model_portfolio,
)
from tests.helpers import EXAMPLE_CONFIG


def test_current_configuration_compiles_to_plural_portfolio() -> None:
    config = load_config(EXAMPLE_CONFIG).config
    portfolio = compile_model_portfolio(config)

    assert portfolio.mandatory_baseline_id == config.models.fellegi_sunter.model_id
    assert portfolio.pair_candidates[0].family == "fellegi_sunter"
    assert any(candidate.family == "xgboost" for candidate in portfolio.pair_candidates)
    assert any(candidate.family == "xgboost" for candidate in portfolio.ranking_candidates)
    assert portfolio.test_partition_may_select_portfolio is False
    assert portfolio.decision_authority == "none"
    assert portfolio.merge_authority == "none"
    assert len(portfolio.portfolio_digest) == 64


def test_explicit_portfolio_supports_multiple_challengers_and_stacking() -> None:
    portfolio = ModelPortfolioDeclaration(
        portfolio_id="portfolio_demo",
        pair_candidates=(
            PairModelCandidateDeclaration(
                model_id="fs_baseline",
                family="fellegi_sunter",
                implementation="mapel_reference_fellegi_sunter",
                role="baseline",
                require_verified_labels=False,
                artifact_format="package_json",
            ),
            PairModelCandidateDeclaration(
                model_id="xgb_candidate",
                family="xgboost",
                implementation="xgboost_classifier",
                role="challenger",
                require_verified_labels=True,
                artifact_format="xgboost_json",
            ),
            PairModelCandidateDeclaration(
                model_id="lgb_candidate",
                family="lightgbm",
                implementation="lightgbm_classifier",
                role="challenger",
                require_verified_labels=True,
                artifact_format="lightgbm_text",
            ),
            PairModelCandidateDeclaration(
                model_id="stacked_candidate",
                family="stacking",
                implementation="stacking_logistic",
                role="ensemble",
                require_verified_labels=True,
                artifact_format="package_json",
                base_model_ids=("xgb_candidate", "lgb_candidate"),
            ),
        ),
        ranking_candidates=(
            RankingCandidateDeclaration(
                model_id="xgb_ranker",
                family="xgboost",
                implementation="xgboost_ranker",
                query_side="source",
                top_k=5,
                artifact_format="xgboost_json",
            ),
            RankingCandidateDeclaration(
                model_id="lgb_ranker",
                family="lightgbm",
                implementation="lightgbm_ranker",
                query_side="source",
                top_k=5,
                artifact_format="lightgbm_text",
            ),
        ),
        mandatory_baseline_id="fs_baseline",
        maximum_challengers=3,
    )

    assert len(portfolio.pair_candidates) == 4
    assert len(portfolio.ranking_candidates) == 2
    assert portfolio.safe_summary()["pair_candidate_count"] == 4


def test_portfolio_rejects_duplicate_ids_and_invalid_stacking_references() -> None:
    baseline = PairModelCandidateDeclaration(
        model_id="fs_baseline",
        family="fellegi_sunter",
        implementation="mapel_reference_fellegi_sunter",
        role="baseline",
        require_verified_labels=False,
        artifact_format="package_json",
    )
    duplicate = PairModelCandidateDeclaration(
        model_id="fs_baseline",
        family="xgboost",
        implementation="xgboost_classifier",
        role="challenger",
        require_verified_labels=True,
        artifact_format="xgboost_json",
    )
    with pytest.raises(ValidationError):
        ModelPortfolioDeclaration(
            portfolio_id="duplicate_demo",
            pair_candidates=(baseline, duplicate),
            mandatory_baseline_id="fs_baseline",
        )

    invalid_stack = PairModelCandidateDeclaration(
        model_id="stacked_candidate",
        family="stacking",
        implementation="stacking_logistic",
        role="ensemble",
        require_verified_labels=True,
        artifact_format="package_json",
        base_model_ids=("missing_one", "missing_two"),
    )
    with pytest.raises(ValidationError):
        ModelPortfolioDeclaration(
            portfolio_id="invalid_stack_demo",
            pair_candidates=(baseline, invalid_stack),
            mandatory_baseline_id="fs_baseline",
        )
''', encoding='utf-8')

          (root / 'tests/pipeline/test_recipe_io.py').write_text(r'''from __future__ import annotations

import json

import pytest

from mapel_linkage.domain.errors import PipelineError
from mapel_linkage.pipeline import (
    OperationalValidationStatus,
    PipelineRecipeArtifact,
    RecipeApprovalStatus,
    RecipeExecutionMode,
)
from mapel_linkage.pipeline.recipe_io import (
    deserialize_pipeline_recipe,
    serialize_pipeline_recipe,
)


def recipe(*, approved: bool = False) -> PipelineRecipeArtifact:
    return PipelineRecipeArtifact(
        recipe_id="recipe_demo",
        recipe_version="v1",
        linkage_mode="link_only",
        assignment_constraint="one_to_one",
        configuration_digest="a" * 64,
        candidate_plan_digest="b" * 64,
        feature_schema_digest="c" * 64,
        champion_model_id="xgb_candidate",
        champion_model_version="m2-xgboost-v1",
        champion_artifact_digest="d" * 64,
        calibrator_digest="e" * 64,
        ranking_artifact_digest="f" * 64,
        decision_policy_digest="1" * 64,
        validation_evidence_digest="2" * 64,
        approval_status=(
            RecipeApprovalStatus.APPROVED_FOR_INFERENCE
            if approved
            else RecipeApprovalStatus.SYNTHETIC_VALIDATED
        ),
        operational_validation=(
            OperationalValidationStatus.LOCALLY_ESTABLISHED
            if approved
            else OperationalValidationStatus.NOT_ESTABLISHED
        ),
    )


def test_recipe_json_round_trip_is_canonical_and_value_safe() -> None:
    original = recipe()
    payload = serialize_pipeline_recipe(original)
    restored = deserialize_pipeline_recipe(payload)

    assert restored == original
    assert restored.recipe_digest == original.recipe_digest
    assert payload == serialize_pipeline_recipe(restored)
    assert "candidate pairs" not in payload.lower()
    assert "private/" not in payload


def test_recipe_json_rejects_tampering_unknown_and_duplicate_keys() -> None:
    payload = json.loads(serialize_pipeline_recipe(recipe()))
    payload["champion_artifact_digest"] = "9" * 64
    with pytest.raises(PipelineError, match="ML-RECIPE-012"):
        deserialize_pipeline_recipe(json.dumps(payload))

    payload = json.loads(serialize_pipeline_recipe(recipe()))
    payload["unexpected"] = True
    with pytest.raises(PipelineError, match="ML-RECIPE-010"):
        deserialize_pipeline_recipe(json.dumps(payload))

    with pytest.raises(PipelineError, match="ML-RECIPE-006"):
        deserialize_pipeline_recipe('{"schema_version":"1","schema_version":"1"}')


def test_only_locally_validated_approved_recipe_can_control_inference() -> None:
    synthetic = deserialize_pipeline_recipe(serialize_pipeline_recipe(recipe()))
    with pytest.raises(PipelineError, match="ML-RECIPE-005"):
        synthetic.assert_usable_for(RecipeExecutionMode.INFERENCE)

    approved = deserialize_pipeline_recipe(serialize_pipeline_recipe(recipe(approved=True)))
    approved.assert_usable_for(RecipeExecutionMode.INFERENCE)
    assert approved.merge_authority == "none"
''', encoding='utf-8')

          pipeline_init = root / 'src/mapel_linkage/pipeline/__init__.py'
          pipeline_init.write_text(r'''"""Package-owned orchestration and immutable pipeline approval contracts."""

from mapel_linkage.pipeline.contracts import StageSummary, SyntheticVerticalSliceResult
from mapel_linkage.pipeline.model_portfolio import (
    ModelPortfolioDeclaration,
    PairModelCandidateDeclaration,
    RankingCandidateDeclaration,
    compile_model_portfolio,
)
from mapel_linkage.pipeline.recipe_io import (
    deserialize_pipeline_recipe,
    pipeline_recipe_payload,
    serialize_pipeline_recipe,
)
from mapel_linkage.pipeline.recipes import (
    OperationalValidationStatus,
    PipelineRecipeArtifact,
    RecipeApprovalStatus,
    RecipeExecutionMode,
)
from mapel_linkage.pipeline.synthetic_vertical_slice import SyntheticVerticalSliceRunner

__all__ = [
    "ModelPortfolioDeclaration",
    "OperationalValidationStatus",
    "PairModelCandidateDeclaration",
    "PipelineRecipeArtifact",
    "RankingCandidateDeclaration",
    "RecipeApprovalStatus",
    "RecipeExecutionMode",
    "StageSummary",
    "SyntheticVerticalSliceResult",
    "SyntheticVerticalSliceRunner",
    "compile_model_portfolio",
    "deserialize_pipeline_recipe",
    "pipeline_recipe_payload",
    "serialize_pipeline_recipe",
]
''', encoding='utf-8')

          doc = root / 'docs/implementation/MODEL_PORTFOLIO_AND_RECIPE_FOUNDATION.md'
          doc.write_text(r'''# Model Portfolio and Approved Recipe Foundation

## Status

Implemented as a bounded post-audit foundation. This milestone does not provide an operational data runner and does not approve any model for real-world linkage.

## Purpose

The earlier synthetic vertical slice represented one Fellegi-Sunter model, one boosted challenger, and one candidate ranker. The platform now needs a stable plural contract before configuration and orchestration are expanded to run LightGBM, PyTorch, and stacking candidates in a protected tournament.

`ModelPortfolioDeclaration` therefore represents:

- one mandatory baseline;
- zero or more bounded pair-model challengers;
- optional stacking candidates with explicit base-model IDs;
- zero or more candidate-ranker alternatives;
- a maximum challenger count;
- explicit shadow-scoring permission;
- a prohibition on using the locked test partition for portfolio selection;
- no decision, assignment, recommendation, or merge authority.

`compile_model_portfolio()` converts the current backward-compatible project configuration into this plural downstream contract. A later schema revision can expose the plural structure directly without changing the orchestration interface.

## Recipe persistence

The initial `PipelineRecipeArtifact` can now be serialized to and restored from canonical JSON. The loader:

- applies a strict size limit;
- rejects duplicate and unknown keys;
- rejects unsupported schema versions;
- reconstructs only package-owned typed fields;
- verifies the stored recipe digest;
- contains no arbitrary code, import path, SQL, row value, identifier, candidate pair, or filesystem path.

A recipe remains unusable for inference unless it is explicitly `approved_for_inference` and records `locally_established` operational validation. Synthetic validation alone cannot grant inference authority.

## Authority boundary

```text
portfolio recommendation / selection -> evidence planning only
pair models                          -> evidence only
rankers                              -> ordering only
recipe                               -> binds approved artifacts and policy
assignment                           -> global selection only
relationship policy                  -> explicit status decision
merge authority                      -> none
```

No recipe creates a consolidated master record.

## Next integration

1. expose plural candidates through a versioned project-configuration schema;
2. generate immutable stage artifacts and out-of-fold prediction manifests;
3. implement protected portfolio training and validation-only selection;
4. bind the selected champion, calibrator, ranker, assignment, and decision policy into a recipe;
5. implement a separate recipe-driven inference runner with optional challenger shadow scoring;
6. retain explicit local approval and locked-test evidence before inference use.
''', encoding='utf-8')

          readme = root / 'README.md'
          text = readme.read_text(encoding='utf-8')
          marker = '## Command line\n'
          addition = '''## Model portfolio and approved recipes\n\nThe post-audit foundation normalizes the current configured adapters into a plural `ModelPortfolioDeclaration` and provides strict digest-checked JSON serialization for `PipelineRecipeArtifact`. Portfolio declarations have no decision or merge authority, and a recipe cannot control new-data inference until separately approved local validation is recorded. See [`docs/implementation/MODEL_PORTFOLIO_AND_RECIPE_FOUNDATION.md`](docs/implementation/MODEL_PORTFOLIO_AND_RECIPE_FOUNDATION.md).\n\n'''
          if addition not in text:
              text = text.replace(marker, addition + marker)
          readme.write_text(text, encoding='utf-8')

          changelog = root / 'CHANGELOG.md'
          text = changelog.read_text(encoding='utf-8')
          marker = '## [Unreleased]\n'
          addition = '''\n### Added — model portfolio and approved recipe foundation\n\n- Strict plural pair-model and ranker portfolio declarations with one mandatory baseline, bounded challengers, optional stacking base-model references, deterministic digests, and no identity authority.\n- Backward-compatible compiler from the current project configuration into the plural portfolio contract.\n- Canonical non-executable JSON serialization and loading for pipeline recipes with duplicate-key, unknown-field, schema-version, size, and integrity checks.\n- Approval-gated development, shadow, and inference recipe authority.\n- Synthetic-only portfolio, recipe round-trip, tamper, duplicate-key, and inference-authority tests.\n\n'''
          if addition.strip() not in text:
              text = text.replace(marker, marker + addition)
          changelog.write_text(text, encoding='utf-8')

          docs_index = root / 'docs/README.md'
          text = docs_index.read_text(encoding='utf-8')
          marker = '- [`implementation/M2_COMPLETE_SYNTHETIC_MVP.md`](implementation/M2_COMPLETE_SYNTHETIC_MVP.md)\n'
          addition = marker + '- [`implementation/MODEL_PORTFOLIO_AND_RECIPE_FOUNDATION.md`](implementation/MODEL_PORTFOLIO_AND_RECIPE_FOUNDATION.md)\n'
          if 'MODEL_PORTFOLIO_AND_RECIPE_FOUNDATION.md' not in text:
              text = text.replace(marker, addition)
          docs_index.write_text(text, encoding='utf-8')

          for path in ('src/mapel_linkage/__init__.py', 'pyproject.toml', 'tests/test_package_metadata.py'):
              target = root / path
              text = target.read_text(encoding='utf-8')
              text = text.replace('0.2.0.dev1', '0.2.0.dev2')
              target.write_text(text, encoding='utf-8')
          PY
      - name: Install full development and optional-model environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install -c constraints/ci-py312.txt -e ".[core,dev,lightgbm,neural]"
      - name: Format, regenerate derived contracts, and verify
        run: |
          ruff format .
          python scripts/generate_config_schema.py
          python scripts/generate_capability_matrix.py
          python scripts/generate_repository_manifest.py
          python scripts/verify_repository.py
          ruff format --check .
          ruff check .
          mypy src tests
          pytest -q
          python -m build
          python scripts/verify_repository.py --distribution dist
      - name: Commit verified foundation and remove this workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add model portfolio and recipe foundation"
          git push origin HEAD:agent/model-portfolio-recipe-orchestration
