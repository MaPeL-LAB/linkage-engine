# Reproducible Constraints

Exact, tested Python 3.12 dependency resolutions will be added after the compatibility exercise. Package metadata uses ranges; validated CI and local execution should use a separately generated exact constraints file and record all resolved versions in run manifests.
