# Source-to-canonical mapping worksheet

Complete this worksheet only in the authorised local environment.

| Canonical variable ID | Data type | Source dataset column | Target dataset column | Normalisation | Missingness policy | Restricted output permitted? | Approval/evidence |
|---|---|---|---|---|---|---|---|
| `REPLACE_LOCALLY` | `string/date/integer/float/categorical/boolean` | `REPLACE_LOCALLY` | `REPLACE_LOCALLY` | `REPLACE_LOCALLY` | `REPLACE_LOCALLY` | `yes/no` | `REPLACE_LOCALLY` |

Do not commit the completed worksheet. Source-system names and columns may be locally
sensitive even when they do not contain row-level values.
